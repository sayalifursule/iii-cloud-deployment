name: math-worker

runtime:
  language: python
  package_manager: pip
  entry: math_worker.py

sandbox:
  enabled: false

scripts:
  install: "pip install -r requirements.txt"
  start: "watchfiles 'python math_worker.py'"
